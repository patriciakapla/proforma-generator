# Proforma PDF Generator (CLI)

A command-line tool to generate proforma PDFs from structured JSON data.

It allows you to:

- Select a contract file
- Choose a milestone from a payment schedule
- Calculate billing values (percentages and adjustments)
- Generate a formatted PDF for the selected billing
- Update the billing status in the source JSON file
